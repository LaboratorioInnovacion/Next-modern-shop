name: Deploy to Production Server

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy Application
    runs-on: self-hosted
    timeout-minutes: 15
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Listar archivos y carpetas antes del build
      run: |
        echo "Contenido de modern-shopp antes del build:"
        cd /home/psiconervio/ecomerce/Next-modern-shop/modern-shopp
        ls -l
        echo "Contenido de prisma:"
        ls -l prisma || echo "No existe carpeta prisma"
    
    - name: Verificar versión de Node en Dockerfile
      run: |
        echo "Dockerfile base (debería ser node:20-alpine):"
        grep FROM Dockerfile || true
        echo "---"
        echo "🚀 Iniciando despliegue local en $(date)"
        cd /home/psiconervio/ecomerce/Next-modern-shop/modern-shopp

        # Crear .env si no existe
        if [ ! -f ".env" ]; then
          echo "📝 Creando .env..."
          cat > .env << 'EOF'
        NODE_ENV=production
        DATABASE_URL=postgresql://postgres:postgres_secure_2024@postgres:5432/ecommerce
        NEXTAUTH_URL=https://tu-dominio-cloudflare.com
        NEXTAUTH_SECRET=mi_secreto_super_seguro_2024
        SECRET=mi_secreto_super_seguro_2024
        MERCADOPAGO_ACCESS_TOKEN=APP_USR-3904014073520705-081821-ac9642583a0c52da7c3d6aae0fa8eafe-2631277847
        NEXT_PUBLIC_URL=https://tu-dominio-cloudflare.com
        POSTGRES_PASSWORD=postgres_secure_2024
        EOF
        fi

        # Parar contenedores existentes
        echo "⏹️ Parando contenedores..."
        docker-compose -f docker-compose.prod.yml down 2>/dev/null || true

        # Limpiar Docker completamente
        echo "🧹 Limpiando Docker y cache..."
        docker system prune -af
        docker builder prune -af

        # Levantar contenedores SIN CACHE
        echo "🏗️ Construyendo desde cero (sin cache)..."
        docker-compose -f docker-compose.prod.yml build --no-cache

        # Mostrar versión de Node en el contenedor construido
        echo "---"
        echo "Versión de Node en el contenedor nextjs:"
        docker run --rm ecommerce_app_prod node -v || echo "No se pudo obtener versión"

        docker-compose -f docker-compose.prod.yml up -d

        echo "✅ Despliegue completado!"
        echo "📊 Estado de contenedores:"
        docker-compose -f docker-compose.prod.yml ps