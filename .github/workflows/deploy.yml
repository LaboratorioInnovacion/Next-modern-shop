name: Deploy to Production Server

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy Application
    runs-on: self-hosted
    timeout-minutes: 30
    
    env:
      TUNNEL_TOKEN: ${{ secrets.TUNNEL_TOKEN }}
      POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
      NEXTAUTH_SECRET: ${{ secrets.NEXTAUTH_SECRET }}
      SECRET: ${{ secrets.SECRET }}
      MERCADOPAGO_ACCESS_TOKEN: ${{ secrets.MERCADOPAGO_ACCESS_TOKEN }}
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Verificar configuración del túnel
      run: |
        echo "🔍 Verificando configuración del túnel..."
        cd modern-shopp
        
        # Verificar que las variables estén disponibles
        if [ -z "$TUNNEL_TOKEN" ]; then
          echo "❌ TUNNEL_TOKEN no está configurado"
          exit 1
        fi
        
        echo "✅ Variables de entorno configuradas correctamente"

    - name: Crear archivo .env.production
      run: |
        echo "📝 Creando .env.production..."
        cd modern-shopp
        
        cat > .env.production << EOF
        NODE_ENV=production
        DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD:-postgres}@postgres:5432/ecommerce
        NEXTAUTH_URL=https://ecomerce.noaservice.org
        NEXTAUTH_SECRET=${NEXTAUTH_SECRET:-MISECRETO}
        SECRET=${SECRET:-MISECRETO}
        MERCADOPAGO_ACCESS_TOKEN=${MERCADOPAGO_ACCESS_TOKEN:-APP_USR-3904014073520705-081821-ac9642583a0c52da7c3d6aae0fa8eafe-2631277847}
        NEXT_PUBLIC_URL=https://ecomerce.noaservice.org
        POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-postgres}
        TUNNEL_TOKEN=${TUNNEL_TOKEN}
        EOF
        
        echo "✅ Archivo .env.production creado"

    - name: Parar servicios existentes
      run: |
        echo "⏹️ Parando contenedores existentes..."
        cd modern-shopp
        docker-compose -f docker-compose.prod.yml down 2>/dev/null || true
        
        echo "🧹 Limpiando Docker..."
        docker system prune -af
        docker builder prune -af

    - name: Construir y desplegar aplicación
      run: |
        echo "🏗️ Iniciando construcción y despliegue..."
        cd modern-shopp
        
        # Descargar imágenes base
        echo "📥 Descargando imágenes base..."
        docker pull postgres:15-alpine
        docker pull cloudflare/cloudflared:latest
        
        # Construir aplicación sin cache
        echo "🔨 Construyendo aplicación..."
        docker-compose -f docker-compose.prod.yml build --no-cache --pull
        
        # Levantar servicios
        echo "🚀 Levantando servicios..."
        docker-compose -f docker-compose.prod.yml up -d
        
        echo "✅ Despliegue completado!"

    - name: Verificar estado del despliegue
      run: |
        echo "📊 Verificando estado de contenedores..."
        cd modern-shopp
        
        # Esperar un poco para que se inicien
        sleep 30
        
        # Mostrar estado de contenedores
        docker-compose -f docker-compose.prod.yml ps
        
        # Verificar logs del túnel
        echo "🔍 Verificando logs del túnel Cloudflare..."
        docker logs cloudflared_tunnel --tail 20
        
        # Verificar conectividad local
        echo "🌐 Verificando conectividad local..."
        curl -f http://localhost:8081/health || echo "⚠️ Aplicación no responde localmente"
        
        echo "✅ Verificación completada"

    - name: Mostrar información de despliegue
      run: |
        echo "🎉 Despliegue completado exitosamente!"
        echo ""
        echo "� Información del despliegue:"
        echo "  🌐 URL pública: https://ecomerce.noaservice.org"
        echo "  🐳 Contenedores activos:"
        cd modern-shopp
        docker-compose -f docker-compose.prod.yml ps --format table
        echo ""
        echo "📊 Para monitorear:"
        echo "  Ver logs: docker-compose -f docker-compose.prod.yml logs -f"
        echo "  Estado: docker-compose -f docker-compose.prod.yml ps"